# БЭКТАСК-REQUIREMENTS.md

**Обязательные элементы, принципы работы и критерии приемки**

📖 **Навигация:** [БЭКТАСК.MD](./БЭКТАСК.MD) | [БЭКТАСК-PROCESS.md](./БЭКТАСК-PROCESS.md) | [БЭКТАСК-FAQ.md](./БЭКТАСК-FAQ.md) | [БЭКТАСК-ARCHITECTURE.md](./БЭКТАСК-ARCHITECTURE.md)

---

## Стек технологий 2025

**ОБЯЗАТЕЛЬНО использовать современный проверенный стек технологий:**

### Язык и версия
- **Java 21+** - современная версия языка (2025)

### Фреймворк
- **Spring Boot 3.2+** - основной фреймворк для разработки
- **Spring MVC** - для REST API
- **Spring Data JPA** - для работы с БД
- **Spring Security** - для безопасности

### База данных
- **PostgreSQL 15+** - современная версия БД
- **Spring Data JPA / Hibernate** (рекомендуется) - ORM для работы с БД
- **JDBC** - альтернативный подход (если нужен прямой доступ)

### Логирование
- **Logback** (рекомендуется) - структурированное логирование (встроен в Spring Boot)
- **Log4j2** - альтернативное логирование

### Линтинг и форматирование
- **Checkstyle** (рекомендуется) - проверка стиля кода
- **SpotBugs** - статический анализ
- **Spotless** - форматирование кода

### Тестирование
- **JUnit 5** - фреймворк тестирования
- **Mockito** (рекомендуется) - моки для тестирования
- **Spring Boot Test** - интеграционное тестирование

### Валидация
- **Bean Validation (JSR-303)** - валидация данных (встроена в Spring Boot)

### Аутентификация
- **Spring Security** - безопасность и аутентификация
- **JWT (io.jsonwebtoken)** - работа с JWT токенами
- **BCrypt** - хеширование паролей (встроен в Spring Security)

### Генерация кода
- **OpenAPI Generator** (рекомендуется) - генерация Java Spring Boot кода
- **Maven Plugin** - автоматическая генерация при сборке

### Миграции БД
- **Flyway** (рекомендуется) - инструмент для миграций БД
- **Liquibase** - альтернативный инструмент для миграций

---

## Обязательные элементы бекенд кода

Каждый созданный бекенд код должен содержать:

### 1. Генерация Java Spring Boot кода

- Использование OpenAPI Generator для генерации Java Spring Boot кода из API Swagger YAML файлов
- Серверный код должен быть полностью сгенерирован
- Все модели данных (DTOs) должны быть сгенерированы
- Все Controller интерфейсы должны быть сгенерированы
- Сгенерированный код должен быть в `target/generated-sources/openapi/`

### 2. Размер файлов (ОБЯЗАТЕЛЬНО)

- **МАКСИМУМ 400 строк на файл** - это жесткое ограничение
- Если файл превышает 400 строк - **ОБЯЗАТЕЛЬНО** разбить на несколько файлов
- Логическое разбиение:
  - Разделить Controllers по методам (GET, POST, PUT, DELETE)
  - Вынести бизнес-логику в сервисы
  - Вынести работу с БД в репозитории
  - Разделить на подфайлы по функциональности
- Примеры разбиения:
  - `PersonalNpcController.java` (основной Controller, до 400 строк)
  - `PersonalNpcControllerGet.java` (GET методы, до 400 строк)
  - `PersonalNpcControllerPost.java` (POST методы, до 400 строк)
  - `PersonalNpcService.java` (сервис, до 400 строк)
  - `PersonalNpcRepository.java` (репозиторий, до 400 строк)

### 3. Controllers для обработки HTTP запросов

- Controllers для всех endpoints из API Swagger (реализация сгенерированных интерфейсов)
- Обработка параметров запроса (path, query, body)
- Валидация входных данных через Bean Validation
- Обработка ошибок с правильными HTTP кодами
- Использование Spring Security для аутентификации, Spring AOP для логирования

### 4. Services для бизнес-логики

- Сервисы для бизнес-логики приложения
- Инкапсуляция бизнес-правил
- Использование репозиториев для работы с БД
- Обработка транзакций для критических операций
- Обработка ошибок и логирование

### 5. Repositories для работы с БД

- Spring Data JPA интерфейсы для работы с базой данных PostgreSQL
- Использование Spring Data JPA / Hibernate для работы с БД
- Автоматическая генерация реализации из интерфейсов
- Обработка ошибок БД через Spring Data JPA
- Управление транзакциями через Spring

### 6. Миграции БД

- Миграции для создания схемы БД через Flyway
- Использование Flyway для миграций
- Версионирование миграций (V{версия}__{название}.sql)
- Миграции находятся в `src/main/resources/db/migration/`
- Миграции не должны превышать 400 строк (если больше - разбить)
- Flyway применяет миграции автоматически при запуске Spring Boot

### 7. Обработка ошибок

- Все ошибки должны обрабатываться корректно
- Правильные HTTP коды ответов (400, 401, 403, 404, 409, 422, 500)
- Структурированное логирование ошибок (Logback, встроен в Spring Boot)
- Сообщения об ошибках для клиента
- Обработка ошибок БД

### 8. Логирование

- Структурированное логирование (Logback, встроен в Spring Boot)
- Логирование запросов и ответов через Spring AOP
- Логирование ошибок
- Логирование критических операций

### 9. Spring Security и AOP

- Spring Security для аутентификации (Bearer token, JWT)
- Spring AOP для логирования
- Exception handlers для обработки ошибок (@ControllerAdvice)
- CORS настройки через Spring MVC
- Rate limiting через Spring Security или отдельные библиотеки

### 10. Соответствие API Swagger спецификации

- Все endpoints реализованы правильно
- Все модели данных соответствуют спецификации
- Все параметры и ответы соответствуют спецификации
- Использование правильных типов из сгенерированного кода

### 11. Транзакции

- Использование транзакций для критических операций
- Правильная обработка отката транзакций
- Обработка ошибок в транзакциях

### 12. Тестирование

**ОБЯЗАТЕЛЬНО: Покрытие тестами 50%**
- Unit тесты для controllers, services, repositories (JUnit 5, Mockito)
- Интеграционные тесты для API endpoints (Spring Boot Test)
- Тесты для работы с БД (используя тестовую БД или H2 in-memory)
- Покрытие кода тестами должно быть не менее 50%
- Использование `mvn test` для запуска тестов
- Использование JaCoCo для проверки покрытия

### 13. База данных PostgreSQL

**ОБЯЗАТЕЛЬНО: PostgreSQL в Docker**
- Использование PostgreSQL в качестве основной БД
- Пока используется только локальная БД в Docker
- Можно заранее создать контейнер с PostgreSQL
- Использование Spring Data JPA / Hibernate для работы с БД
- Миграции БД через Flyway
- Все данные хранятся в БД, ничего не хардкодится

### 14. Миграции БД

**ОБЯЗАТЕЛЬНО: Создание миграций для схемы БД**
- Создание миграций для всех таблиц из API Swagger спецификации
- Использование Flyway для миграций
- Версионирование миграций (V{версия}__{название}.sql)
- Миграции находятся в `src/main/resources/db/migration/`
- Миграции не должны превышать 400 строк (если больше - разбить на несколько)
- Миграции должны быть идемпотентными (можно применять несколько раз)
- Flyway применяет миграции автоматически при запуске Spring Boot

### 15. Тестовые данные (Seed данные)

**ОБЯЗАТЕЛЬНО: Создание тестовых данных при необходимости**
- Создание seed данных для тестирования функционала
- Seed данные создаются только если их изначально не было в БД
- **ВАЖНО:** Проверка существования данных перед заливкой - если данные уже есть, не заливать повторно
- Использование миграций или отдельных seed скриптов для создания тестовых данных
- Seed данные должны быть минимальными и достаточными для проверки функционала
- Идемпотентность: можно запускать несколько раз без дублирования данных

### 16. Документация

- JavaDoc комментарии к публичным классам и методам
- Комментарии к публичным методам
- README.md в директориях (если нужно)
- Примеры использования

---

## Принципы работы

1. **Следование API Swagger:** Бекенд код должен точно соответствовать API Swagger спецификации - все endpoints и модели должны быть реализованы

2. **Самодостаточность:** Код должен быть самодостаточным - не должно быть зависимостей от несуществующих компонентов

3. **Java стандарты:** ОБЯЗАТЕЛЬНО соблюдать стандарты Java: Java Code Conventions, Checkstyle, SpotBugs

4. **Полнота:** Все элементы из API Swagger должны быть реализованы - endpoints, модели, примеры, документация

5. **Соответствие принципам:** Код должен соответствовать принципам проекта (SOLID, DRY, KISS)

6. **Версионирование:** ОБЯЗАТЕЛЬНО вести версионирование кода через git - каждый коммит должен быть логичным блоком работы

7. **Ограничение размера:** Каждый файл не должен превышать 400 строк. Если больше - ОБЯЗАТЕЛЬНО разбить на несколько файлов с логической структурой

8. **Иерархия директорий:** ОБЯЗАТЕЛЬНО следовать иерархии директорий - соответствие структуре API-SWAGGER

9. **Генерация кода:** ОБЯЗАТЕЛЬНО использовать OpenAPI Generator для генерации Java Spring Boot кода из API Swagger YAML файлов

10. **Тестирование:** ОБЯЗАТЕЛЬНО покрытие тестами не менее 50% - unit тесты для controllers, services, repositories, интеграционные тесты для API endpoints

11. **PostgreSQL в Docker:** ОБЯЗАТЕЛЬНО использовать PostgreSQL в Docker для локальной разработки - можно заранее создать контейнер

12. **Миграции БД:** ОБЯЗАТЕЛЬНО создавать миграции для всех таблиц из API Swagger спецификации - версионирование, идемпотентность

13. **Тестовые данные:** ОБЯЗАТЕЛЬНО создавать тестовые данные (seed данные) при необходимости - проверка существования перед заливкой, идемпотентность

14. **Данные в БД:** ОБЯЗАТЕЛЬНО все данные хранить в БД, ничего не хардкодить в коде

---

## Контрольный список для агента

### Перед началом работы:

- [ ] Прочитал `back-java-rules.mdc`
- [ ] Прочитал API Swagger спецификацию полностью
- [ ] Понял все endpoints из API
- [ ] Понял все модели данных из API
- [ ] Проверил зависимости (если есть)
- [ ] Понял критерии приемки

### После создания бекенд кода:

- [ ] Java Spring Boot код сгенерирован через OpenAPI Generator
- [ ] Серверный код полностью сгенерирован
- [ ] Controllers созданы и соответствуют API
- [ ] Services созданы для бизнес-логики
- [ ] Repositories созданы для работы с БД
- [ ] **Миграции БД созданы:** Созданы миграции для всех таблиц из API Swagger
- [ ] **Тестовые данные:** Созданы seed данные (если нужно) с проверкой существования
- [ ] Каждый файл не превышает 400 строк (если больше - разбито на несколько файлов)
- [ ] Структура директорий соответствует иерархии API-SWAGGER
- [ ] Код соответствует принципам SOLID, DRY, KISS
- [ ] Код соответствует Java стандартам (Checkstyle, SpotBugs)
- [ ] Обработка ошибок реализована
- [ ] Логирование реализовано
- [ ] Spring Security и AOP реализованы
- [ ] Транзакции используются для критических операций
- [ ] Соответствует API Swagger спецификации
- [ ] Данные не хардкодятся - все хранится в БД
- [ ] **Тестирование:** Покрытие тестами не менее 50% - unit и интеграционные тесты
- [ ] **PostgreSQL в Docker:** Настроена локальная БД в Docker контейнере
- [ ] **Сделан коммит** изменений (для сохранения истории)

---

## Критерии приемки (общие)

Бекенд код считается выполненным, если:

1. ✅ **Java Spring Boot код сгенерирован:** Серверный код сгенерирован через OpenAPI Generator и сохранен в `target/generated-sources/openapi/`
2. ✅ **Размер файлов:** Каждый файл не превышает 400 строк (если больше - разбито на несколько файлов)
3. ✅ **Структура директорий:** Структура соответствует иерархии API-SWAGGER
4. ✅ **Controllers:** Созданы Controllers для всех endpoints из API (реализация сгенерированных интерфейсов)
5. ✅ **Services:** Созданы Services для бизнес-логики
6. ✅ **Repositories:** Созданы Spring Data JPA интерфейсы для работы с БД
7. ✅ **Entities:** Созданы JPA Entities для работы с БД
8. ✅ **Миграции БД:** Созданы Flyway миграции для схемы БД (версионирование, идемпотентность)
9. ✅ **Принципы:** Код соответствует принципам SOLID, DRY, KISS
10. ✅ **Java стандарты:** Код соответствует Java стандартам (Checkstyle, SpotBugs)
11. ✅ **Обработка ошибок:** Все ошибки обрабатываются корректно с правильными HTTP кодами (@ControllerAdvice)
12. ✅ **Логирование:** Реализовано структурированное логирование (Logback, встроен в Spring Boot)
13. ✅ **Spring Security и AOP:** Реализованы Spring Security для аутентификации, Spring AOP для логирования, @ControllerAdvice для обработки ошибок
14. ✅ **Транзакции:** Используются транзакции для критических операций (@Transactional)
15. ✅ **Соответствие API:** Код соответствует API Swagger спецификации
16. ✅ **Данные в БД:** Все данные хранятся в БД, ничего не хардкодится
17. ✅ **Миграции БД:** Созданы Flyway миграции для всех таблиц из API Swagger - версионирование, идемпотентность
18. ✅ **Тестовые данные:** Созданы seed данные (если нужно) с проверкой существования перед заливкой
19. ✅ **Тестирование:** Покрытие тестами не менее 50% - unit и интеграционные тесты (JUnit 5, Mockito, Spring Boot Test)
20. ✅ **PostgreSQL в Docker:** Настроена локальная БД в Docker контейнере
21. ✅ **Документация:** Есть JavaDoc комментарии к публичным классам и методам
22. ✅ **Иерархия:** Строгое соответствие иерархии директорий API-SWAGGER
23. ✅ **Коммит:** Сделан коммит изменений для сохранения истории

---

📖 **Навигация:** [БЭКТАСК.MD](./БЭКТАСК.MD) | [БЭКТАСК-PROCESS.md](./БЭКТАСК-PROCESS.md) | [БЭКТАСК-FAQ.md](./БЭКТАСК-FAQ.md) | [БЭКТАСК-ARCHITECTURE.md](./БЭКТАСК-ARCHITECTURE.md)

