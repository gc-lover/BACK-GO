# БЭКТАСК.MD

**Главный файл документации для AI агента (Backend Agent) - создание бекенд кода на Java Spring Boot на основе API Swagger спецификаций**

---

## 📋 Что это?

Этот файл - точка входа для AI агента, который создает бекенд код на Java Spring Boot на основе API Swagger спецификаций из `API-SWAGGER/api/v1/`.

**Документация разбита на несколько файлов для удобства навигации:**

- **[БЭКТАСК.MD](./БЭКТАСК.MD)** (этот файл) - навигация и быстрое начало
- **[БЭКТАСК-PROCESS.md](./БЭКТАСК-PROCESS.md)** - детальный процесс работы агента (все шаги)
- **[БЭКТАСК-REQUIREMENTS.md](./БЭКТАСК-REQUIREMENTS.md)** - обязательные элементы, принципы, критерии приемки
- **[БЭКТАСК-FAQ.md](./БЭКТАСК-FAQ.md)** - типичные проблемы, решения, примеры использования
- **[БЭКТАСК-ARCHITECTURE.md](./БЭКТАСК-ARCHITECTURE.md)** - архитектура, структура директорий
- **[БЭКТАСК-ENTITY-GENERATION.md](./БЭКТАСК-ENTITY-GENERATION.md)** - автоматическая генерация Entities и Repositories
- **[OPENAPI-GENERATION-GUIDE.md](./OPENAPI-GENERATION-GUIDE.md)** - руководство по генерации кода из OpenAPI
- **[OPENAPI-TO-FULL-CODE-PLAN.md](./OPENAPI-TO-FULL-CODE-PLAN.md)** - план генерации полноценного кода из OpenAPI
- **[QUICK-START.md](./QUICK-START.md)** - быстрый старт проекта
- **[DOCKER-SETUP.md](./DOCKER-SETUP.md)** - настройка Docker для локальной разработки

---

## 🚀 Быстрое начало

### ⚠️ Порядок разработки

**ВАЖНО:** Бекенд разрабатывается ПОСЛЕ API спецификации, но ПЕРЕД фронтендом!

**Цепочка разработки:**
```
.BRAIN → API-SWAGGER → BACK-JAVA → FRONT-WEB
```

**Почему:**
- Бекенд зависит от готовой API спецификации
- Фронтенд зависит от готового бекенда
- Легче тестировать по этапам

**Перед началом работы бекенд-агента убедитесь:**
1. ✅ API Swagger спецификация готова в `API-SWAGGER/api/v1/`
2. ✅ Все endpoints определены в спецификации
3. ✅ Модели данных описаны в API Swagger
4. ✅ PostgreSQL запущен в Docker (через `docker-compose up -d`)

**После завершения работы бекенд-агента:**
1. ✅ Все API endpoints реализованы и работают
2. ✅ БД настроена (миграции применены)
3. ✅ Тестовые данные созданы (seed данные)
4. ✅ API можно протестировать через Postman/curl
5. ✅ Бекенд готов для интеграции с фронтендом

**Подробнее:** См. [DEVELOPMENT-WORKFLOW.md](../DEVELOPMENT-WORKFLOW.md) в корне проекта

---

### Минимальный вход для работы агента:

1. **Этот файл** (БЭКТАСК.MD) - навигация по документации
2. **ПРАВИЛО:** `BACK-JAVA/.cursor/rules/back-java-rules.mdc` (уже включено автоматически)
3. **API SWAGGER:** файлы из `API-SWAGGER/api/v1/*.yaml` или `API-SWAGGER/api/v1/*/*.yaml`
4. **PostgreSQL:** должен быть запущен в Docker (через `docker-compose up -d`)

**Дополнительная информация (опционально):**
- `API-SWAGGER/tasks/completed/*` - выполненные задания для контекста
- Существующий код: `BACK-JAVA/src/main/java/com/necpgame/backjava/*` - примеры существующего кода для стиля и формата

---

## 📖 Как использовать

### Вариант 1: Один API Swagger файл

**Команда:**
```
Делай бекенд для API-SWAGGER/api/v1/gameplay/social/personal-npc-tool.yaml
```

**Что делает агент:**
1. **Проверяет готовность API спецификации:** Убеждается, что API Swagger спецификация готова и полная
2. **Проверяет Docker:** Убеждается, что PostgreSQL запущен в Docker (через `docker-compose up -d`)
3. Читает этот файл (БЭКТАСК.MD) и связанные файлы документации
4. Читает правила из `back-java-rules.mdc`
5. Читает API Swagger спецификацию из указанного файла
6. **Генерирует автоматически:** Запускает `mvn clean generate-sources` для генерации DTOs/Models/Controller интерфейсов/Entities/Repositories из OpenAPI
7. **Создает вручную:** Services, ServiceImpl, Controller реализации, Flyway миграции БД
8. Создает миграции БД для всех таблиц из API Swagger (версионирование, идемпотентность)
9. Создает тестовые данные (seed данные) при необходимости - с проверкой существования
10. Пишет тесты (покрытие 50%)
11. Соблюдает принципы SOLID, DRY, KISS
12. Проверяет размер файлов (не более 400 строк)
13. Тестирует API endpoints (через curl/Postman)
14. Сохраняет код в соответствующей структуре директорий
15. Делает коммит изменений

---

### Вариант 2: Несколько API Swagger файлов

**Команда:**
```
Делай бекенд для всех API из API-SWAGGER/api/v1/gameplay/social/
```

**Что делает агент:**
1. Читает документацию и правила
2. Читает ВСЕ API Swagger файлы из указанной директории
3. Для каждого файла выполняет процесс создания бекенд кода
4. Учитывает зависимости между API (если указаны)

---

### Вариант 3: Все API из директории

**Команда:**
```
Делай бекенд для всех API из API-SWAGGER/api/v1/
```

**Что делает агент:**
1. Сканирует все API Swagger файлы в `API-SWAGGER/api/v1/`
2. Выполняет процесс создания бекенд кода для каждого API
3. Учитывает иерархию директорий

---

## 📚 Навигация по документации

### Основные разделы:

1. **[Процесс работы агента](./БЭКТАСК-PROCESS.md)**
   - Шаг 1: Чтение правил и API Swagger
   - Шаг 2: Генерация Java Spring Boot кода через OpenAPI Generator
   - Шаг 3: Создание Controllers, Services, Repositories
   - Шаг 4: Создание миграций БД
   - Шаг 5: Валидация и проверка
   - Шаг 6: Финализация
   - Шаг 7: Коммит изменений

2. **[Требования и принципы](./БЭКТАСК-REQUIREMENTS.md)**
   - Обязательные элементы бекенд кода
   - Принципы работы (SOLID, DRY, KISS)
   - Критерии приемки (18+ пунктов)
   - Контрольный список для агента

3. **[FAQ и примеры](./БЭКТАСК-FAQ.md)**
   - Типичные проблемы и решения
   - Примеры использования OpenAPI Generator
   - Примеры команд
   - Инструменты для проверки

4. **[Архитектура](./БЭКТАСК-ARCHITECTURE.md)**
   - Архитектура и иерархия директорий
   - Структура файлов
   - Соответствие API-SWAGGER
   - Структура директории с Controllers

---

## 🔗 Ссылки на правила и документацию

### Правила работы

**Файл:** `BACK-JAVA/.cursor/rules/back-java-rules.mdc`

**Содержит:**
- Репозиторий BACK-JAVA: бекенд сервис на Java Spring Boot
- **Стек технологий 2025:**
  - Java 21+ (современная версия)
  - Spring Boot 3.2+ - фреймворк
  - PostgreSQL 15+ с Spring Data JPA / Hibernate
  - Logback для логирования
  - Maven для сборки
  - JUnit 5 для тестирования
  - OpenAPI Generator для генерации кода (Java Spring Boot)
  - Flyway для миграций БД
- REST API для внешних вызовов, gRPC для внутренних сервисов
- Генерация кода из API-SWAGGER спецификаций
- Middleware для обработки ошибок, логирования, аутентификации
- Принципы проекта: SOLID, DRY, KISS
- Ограничение размера файла: МАКСИМУМ 400 строк на файл
- Данные не хардкодить - все хранить в БД

**Читать:** [Правила работы](./.cursor/rules/back-java-rules.mdc)

---

## ⚡ Ключевые моменты

### ОБЯЗАТЕЛЬНО:

1. ✅ **Проверь готовность API спецификации:** Убедись, что API Swagger спецификация готова и полная
2. ✅ **Используй OpenAPI Generator** для генерации Java Spring Boot кода из API Swagger YAML файлов
3. ✅ **Ограничение размера:** МАКСИМУМ 400 строк на файл. Если больше - ОБЯЗАТЕЛЬНО разбить на несколько файлов
4. ✅ **Создай директорию и README.md** если файлов несколько
5. ✅ **Соблюдай принципы:** SOLID, DRY, KISS
6. ✅ **Следуй иерархии директорий** - соответствие структуре API-SWAGGER
7. ✅ **Используй миграции БД** для создания схемы БД (версионирование, идемпотентность)
8. ✅ **Создай тестовые данные (seed данные)** при необходимости - с проверкой существования перед заливкой
9. ✅ **Тестирование:** Покрытие тестами не менее 50% - unit и интеграционные тесты
10. ✅ **PostgreSQL в Docker:** Используй PostgreSQL в Docker для локальной разработки
11. ✅ **Данные не хардкодить** - все хранить в БД
12. ✅ **Проверь работу API:** Все endpoints должны работать и быть доступны для фронтенда
13. ✅ **Сделай коммит** после завершения работы

---

## 📝 Примеры команд

```
# Один API файл
Делай бекенд для API-SWAGGER/api/v1/gameplay/social/personal-npc-tool.yaml

# Директория с API
Делай бекенд для всех API из API-SWAGGER/api/v1/gameplay/social/

# Все API
Делай бекенд для всех API из API-SWAGGER/api/v1/
```

**Подробнее:** [БЭКТАСК-FAQ.md](./БЭКТАСК-FAQ.md#примеры-команд)

---

## 🎯 Что делать дальше?

1. **Прочитай:** [БЭКТАСК-PROCESS.md](./БЭКТАСК-PROCESS.md) - детальный процесс работы
2. **Изучи:** [БЭКТАСК-REQUIREMENTS.md](./БЭКТАСК-REQUIREMENTS.md) - требования и критерии
3. **Используй:** [БЭКТАСК-FAQ.md](./БЭКТАСК-FAQ.md) - если возникли вопросы
4. **Проверь:** [БЭКТАСК-ARCHITECTURE.md](./БЭКТАСК-ARCHITECTURE.md) - архитектуру и структуру

---

## 📌 Важные файлы

**Обязательно прочитать:**
1. `BACK-JAVA/.cursor/rules/back-java-rules.mdc` - правила работы с бекендом
2. API Swagger файл из `API-SWAGGER/api/v1/*/*.yaml` - спецификация API

**Использовать:**
- `BACK-JAVA/src/main/java/com/necpgame/backjava/*` - примеры существующего кода (для стиля, если есть)
- `API-SWAGGER/api/v1/*` - все API Swagger спецификации
- `BACK-JAVA/docker-compose.yml` - Docker конфигурация с PostgreSQL (запустить через `docker-compose up -d`)

**Результат генерации (автоматически из OpenAPI):**
- ✅ `target/generated-sources/openapi/*` - DTOs/Models/Controller реализации (стандартные шаблоны OpenAPI Generator)
- ✅ `target/generated-sources/entities/*` - JPA Entities (кастомный шаблон `Entity.mustache`)
- ✅ `target/generated-sources/repositories/*` - Spring Data JPA Repositories (скрипт `generate-repositories.ps1`)
- ✅ `target/generated-sources/services/*` - Services (скрипт `generate-services.ps1`)
- ✅ `target/generated-sources/services/impl/*` - ServiceImpl (скрипт `generate-services.ps1`)

**Результат реализации (создаются вручную):**
- ❌ `src/main/resources/db/changelog/*` - Liquibase changelog (создаются вручную или через генератор)

**⚠️ ВАЖНО:** 
- ✅ **Controller реализации генерируются автоматически!** (используется кастомный шаблон `apiController.mustache`)
- ✅ **Services и ServiceImpl генерируются автоматически!** (скрипт `generate-services.ps1` из контроллеров)
- ✅ **JPA Entities генерируются автоматически!** (используется кастомный шаблон `model.mustache`)
- 🔄 **Liquibase changelog генерируется через LiquibaseChangelogGenerator** (из JPA Entities)
- 📝 Сгенерированные контроллеры имеют пустые реализации методов - их нужно дополнить бизнес-логикой через Services
- 📝 Сгенерированные Services и ServiceImpl имеют пустые реализации методов - их нужно дополнить бизнес-логикой через Repositories

**Команда генерации:**
- `mvn clean generate-sources` - автоматическая генерация DTOs/Models/Controller реализаций/Entities/Repositories из OpenAPI

**⚠️ ВАЖНО:** 
- ✅ **Services и ServiceImpl генерируются автоматически!** (скрипт `generate-services.ps1`)
- ✅ **JPA Entities генерируются автоматически!** (шаблон `model.mustache`)
- 🔄 **Liquibase changelog генерируется через LiquibaseChangelogGenerator** (из JPA Entities)
- Эти компоненты нужно создавать вручную после генерации базового кода
- Подробнее: [OPENAPI-TO-FULL-CODE-PLAN.md](./OPENAPI-TO-FULL-CODE-PLAN.md)

---

## 🔗 Полезные ссылки

### Документация проекта
- [DEVELOPMENT-WORKFLOW.md](../DEVELOPMENT-WORKFLOW.md) - порядок разработки проекта
- [ФРОНТТАСК.MD](../FRONT-WEB/ФРОНТТАСК.MD) - документация для фронтенд агента
- [АПИТАСК.MD](../API-SWAGGER/АПИТАСК.MD) - документация для API агента

### Внешние ресурсы
- [OpenAPI Generator](https://openapi-generator.tech/)
- [Spring Boot Documentation](https://spring.io/projects/spring-boot)
- [Spring Data JPA](https://spring.io/projects/spring-data-jpa)
- [Flyway](https://flywaydb.org/)
- [Maven](https://maven.apache.org/)
- [PostgreSQL JDBC Driver](https://jdbc.postgresql.org/)
- [Java Documentation](https://docs.oracle.com/en/java/)

---

**Готов к работе! Пришли мне этот файл и путь к API Swagger спецификации - я создам бекенд код на Java Spring Boot.**

