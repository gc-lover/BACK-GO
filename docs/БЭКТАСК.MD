# БЭКТАСК.MD

**Главный файл документации для AI агента (Backend Agent) - создание бекенд кода на Java Spring Boot на основе OpenAPI спецификаций**

---

## 📋 Что это?

Этот файл - точка входа для AI агента, который создает бекенд код на Java Spring Boot на основе OpenAPI спецификаций из `API-SWAGGER/api/v1/`.

**Документация:**
- **[БЭКТАСК.MD](./БЭКТАСК.MD)** (этот файл) - главная навигация и быстрое начало
- **[MANUAL-TEMPLATES.md](./MANUAL-TEMPLATES.md)** - готовые шаблоны для ручного создания реализации
- **[БЭКТАСК-ARCHITECTURE.md](./БЭКТАСК-ARCHITECTURE.md)** - архитектура проекта и структура директорий
- **[БЭКТАСК-FAQ.md](./БЭКТАСК-FAQ.md)** - часто задаваемые вопросы и примеры
- **[DOCKER-SETUP.md](./DOCKER-SETUP.md)** - настройка Docker для локальной разработки
- **[QUICK-START.md](./QUICK-START.md)** - быстрый старт проекта за 5 минут

---

## 🎯 Философия: Контракты vs Реализация

### ✅ Генерируется АВТОМАТИЧЕСКИ (контракты):

**OpenAPI спецификация = источник правды ТОЛЬКО для контрактов**

**📁 Контракты генерируются внутри соответствующего микросервиса (`microservices/<service>/src/main/java`) и НЕ удаляются при `mvn clean`**

1. **DTOs** (`microservices/<service>/src/main/java/com/necpgame/<service>/model/`)
   - Модели данных для REST API
   - ✅ Можно безопасно перегенерировать
   - ✅ Никогда не редактируем вручную
   - ✅ Хранятся в каталоге микросервиса

2. **API Interfaces** (`microservices/<service>/src/main/java/com/necpgame/<service>/api/`)
   - Контракты REST API **СО SPRING MVC АННОТАЦИЯМИ**
   - ✅ Содержат @RequestMapping, @RequestParam, @PathVariable, @Valid
   - ✅ Можно безопасно перегенерировать
   - ✅ Никогда не редактируем вручную
   - ✅ Хранятся в каталоге микросервиса
   - ⚠️ **OpenAPI = ЕДИНСТВЕННЫЙ источник истины для REST API!**

3. **Service Interfaces** (`microservices/<service>/src/main/java/com/necpgame/<service>/service/`)
   - Контракты бизнес-логики
   - ✅ Можно безопасно перегенерировать
   - ✅ Никогда не редактируем вручную
   - ✅ Хранятся в каталоге микросервиса

### ✍️ Создаётся ВРУЧНУЮ (реализация):

**Создаются внутри микросервиса и никогда не перегенерируются**

4. **Entities** (`microservices/<service>/src/main/java/com/necpgame/<service>/entity/`)
   - JPA сущности с relationships, indexes, constraints
   - 📝 Используй шаблоны из [MANUAL-TEMPLATES.md](./MANUAL-TEMPLATES.md)

5. **Repositories** (`microservices/<service>/src/main/java/com/necpgame/<service>/repository/`)
   - Spring Data репозитории с custom queries
   - 📝 Используй шаблоны из [MANUAL-TEMPLATES.md](./MANUAL-TEMPLATES.md)

6. **Controllers** (`microservices/<service>/src/main/java/com/necpgame/<service>/controller/`)
   - REST контроллеры, реализующие API Interfaces
   - ⚠️ **ВАЖНО**: Контроллеры ОБЯЗАТЕЛЬНО должны реализовывать API интерфейсы из OpenAPI
   - ⚠️ **НЕ дублируй** Spring MVC аннотации - они УЖЕ в API интерфейсе
   - 📝 Используй шаблоны из [MANUAL-TEMPLATES.md](./MANUAL-TEMPLATES.md)

7. **ServiceImpl** (`microservices/<service>/src/main/java/com/necpgame/<service>/service/impl/`)
   - Реализация бизнес-логики
   - 📝 Используй шаблоны из [MANUAL-TEMPLATES.md](./MANUAL-TEMPLATES.md)

8. **Liquibase миграции** (`microservices/<service>/src/main/resources/db/changelog/`)
   - XML/YAML changelog для создания/изменения схемы БД
   - 📝 Создаются вручную на основе Entities или генерируются через `mvn liquibase:diffChangeLog`

---

## 🚀 Быстрое начало

### ⚠️ Порядок разработки

**ВАЖНО:** Бекенд разрабатывается ПОСЛЕ API спецификации, но ПЕРЕД фронтендом!

**Цепочка разработки:**
```
.BRAIN → API-SWAGGER → BACK-GO (Java) → FRONT-WEB (TypeScript)
```

**Перед началом работы убедитесь:**
1. ✅ OpenAPI спецификация готова в `API-SWAGGER/api/v1/`
2. ✅ Все endpoints определены
3. ✅ Модели данных описаны
4. ✅ PostgreSQL запущен в Docker (`docker-compose up -d`)
5. ✅ **Запись в implementation-tracker.yaml существует** (backend.status: not_started)
6. ✅ Запусти `pwsh -NoProfile -File ..\API-SWAGGER\scripts\validate-swagger.ps1 -ThrottleLimit 8` из корня монорепозитория и убедись, что `swagger-validation.json` не содержит ошибок. Все найденные несоответствия устрани в исходной спецификации, опираясь на требования задания от ДУАПИТАСК и указания менеджера в `.BRAIN`, прежде чем приступать к реализации бекенда.

---

## ⚠️ ВАЖНО: Стандартные URL для API (для справки)

**OpenAPI спецификации используют следующие URL:**

### Production API:
- **HTTP:** `https://api.necp.game/v1`
- **WebSocket:** `wss://api.necp.game/v1`
- **Назначение:** Единая публичная точка входа во все микросервисы

### Локальная разработка (где работает бекенд):
- **API Gateway:** `http://localhost:8080/api/v1` - роутинг ко всем микросервисам
- **Прямой доступ к микросервисам (для отладки):**
  - auth-service: `http://localhost:8081/api/v1`
  - character-service: `http://localhost:8082/api/v1`
  - gameplay-service: `http://localhost:8083/api/v1`
  - social-service: `http://localhost:8084/api/v1`
  - economy-service: `http://localhost:8085/api/v1`
  - world-service: `http://localhost:8086/api/v1`

**⚠️ Примечание:** Backend реализует endpoints на локальных портах (8081-8086). Production URL (`https://api.necp.game/v1`) используется только в OpenAPI спецификациях для документации.

---

## 🔄 Место БЭКТАСК в воркфлоу проекта

БЭКТАСК - это **пятый этап** в цепочке разработки функциональности от идеи до реализации.

### Краткая диаграмма воркфлоу

```
ИДЕЯ/ЗАДАЧА
    ↓
[1] МЕНЕДЖЕР (.BRAIN)
    ↓ Создаёт документы (api-readiness: ready)
    ↓
[2] Brain Readiness Checker (опционально)
    ↓ Проверяет готовность
    ↓
[3] ДУАПИТАСК (API Task Creator)
    ↓ Создаёт задания для API
    ↓
[4] АПИТАСК (API Executor)
    ↓ Создаёт OpenAPI спецификацию
    ↓ СОЗДАЁТ запись в implementation-tracker.yaml
    ↓
[5] БЭКТАСК (Backend Agent) ← ВЫ ЗДЕСЬ
    ↓ ⚠️ ОБНОВЛЯЕТ implementation-tracker.yaml (backend.status: in_progress)
    ↓ Генерирует контракты из OpenAPI
    ↓ Создаёт реализацию (Entities, Repos, Controllers, Services)
    ↓ Создаёт миграции БД
    ↓ Тестирует API
    ↓ ⚠️ ОБНОВЛЯЕТ implementation-tracker.yaml (backend.status: completed)
    ↓
[6] ФРОНТТАСК (Frontend Agent)
    ↓ Проверяет backend.status: completed
    ↓ Реализует frontend
    ↓
✅ ГОТОВАЯ ФИЧА
```

### Откуда берутся задачи (входные данные)

**Источник:** OpenAPI спецификации из `API-SWAGGER/api/v1/` со статусом backend.status: not_started

**Процесс:**
1. **АПИТАСК** создаёт OpenAPI спецификацию
2. **АПИТАСК** создаёт запись в `implementation-tracker.yaml`:
   - api_status: "completed"
   - backend.status: "not_started"
3. **БЭКТАСК** читает `implementation-tracker.yaml`
4. Находит API с backend.status: "not_started"
5. Начинает реализацию backend

**Как найти готовые API:**
- Проверить `implementation-tracker.yaml`
- Найти записи с backend.status: "not_started"

### Что происходит после БЭКТАСК (выходные данные)

**Результат:** Backend реализован, API endpoints работают, ФРОНТТАСК может начинать работу

**Процесс:**
1. **БЭКТАСК** реализует backend
2. Обновляет `implementation-tracker.yaml` (backend.status: "completed")
3. **ФРОНТТАСК** читает `implementation-tracker.yaml`
4. Проверяет, что backend.status: "completed"
5. Начинает реализацию frontend

**Подробнее о воркфлоу:** См. [DEVELOPMENT-WORKFLOW.md](../../DEVELOPMENT-WORKFLOW.md)

---

### 🔄 Генерация кода

**Вариант 1: Через Maven (рекомендуется):**

```bash
cd BACK-GO
mvn clean compile
```

**Вариант 2: Через новый микросервисный скрипт (рекомендуется для OpenAPI генерации):**

```powershell
# Проверка и генерация в микросервисы
cd BACK-GO
pwsh -NoProfile -Command "./scripts/validate-openapi.ps1 -ApiDirectory ../API-SWAGGER/api/v1/"
pwsh -NoProfile -Command "./scripts/generate-openapi-microservices.ps1 -ApiDirectory ../API-SWAGGER/api/v1/"
```

**Что генерируется:**
- ✅ DTOs (модели данных)
- ✅ API Interfaces (REST контракты)
- ✅ Service Interfaces (контракты бизнес-логики)

**После завершения работы:**
1. ✅ Контракты сгенерированы из OpenAPI
2. ✅ Реализация создана вручную (Entities, Repositories, Controllers, ServiceImpl)
3. ✅ БД настроена (миграции применены)
4. ✅ Тестовые данные созданы (seed данные)
5. ✅ API протестирован (curl/Postman)
6. ✅ Код закоммичен

---

## 📖 Как использовать агента

### Команда для агента:

```
Создай бекенд для API-SWAGGER/api/v1/auth/character-creation.yaml
```

### Что делает агент:

#### Шаг 0: ОБЯЗАТЕЛЬНАЯ процедура обновления implementation-tracker.yaml (ПРИ НАЧАЛЕ)

**⚠️ КРИТИЧЕСКИ ВАЖНО:** Этот шаг ОБЯЗАТЕЛЕН перед началом работы!

**При НАЧАЛЕ работы:**

1. **Получить РЕАЛЬНОЕ время из системы:**
   ```powershell
   # Windows
   powershell -Command "Get-Date -Format 'yyyy-MM-dd HH:mm'"
   
   # Linux/Mac
   date '+%Y-%m-%d %H:%M'
   ```

2. **Открыть файл:** `../.BRAIN/06-tasks/config/implementation-tracker.yaml`

3. **Найти запись для API** (по api_path, например "api/v1/auth/character-creation.yaml")

4. **Обновить поля backend:**
   ```yaml
   backend:
     status: "in_progress"  # Было: not_started
     started: "2025-11-07 10:00"  # РЕАЛЬНОЕ время!
     agent: "Backend Agent"
   ```

5. **Сохранить файл**

6. **Сделать коммит обновления:**
   ```powershell
   cd ..
   .BRAIN\scripts\autocommit.ps1 "backend: начата реализация для api/v1/..."
   cd BACK-GO
   ```

**Зачем это нужно:**
- Отслеживание статуса реализации backend
- ФРОНТТАСК видит, что backend в процессе
- Полная видимость процесса разработки

---

#### Шаг 1: Генерация контрактов (автоматически)

**Вариант A: Через Maven (рекомендуется):**
```bash
cd BACK-GO
mvn clean compile
```

**Вариант B: Через микросервисный скрипт:**
```powershell
cd BACK-GO
pwsh -NoProfile -Command "./scripts/validate-openapi.ps1 -ApiDirectory ../API-SWAGGER/api/v1/"
pwsh -NoProfile -Command "./scripts/generate-openapi-microservices.ps1 -ApiDirectory ../API-SWAGGER/api/v1/"
```

**Результат (для каждого микросервиса):**
- ✅ DTOs → `microservices/<service>/src/main/java/com/necpgame/<service>/model/`
- ✅ API Interfaces → `microservices/<service>/src/main/java/com/necpgame/<service>/api/`
- ✅ Service Interfaces → `microservices/<service>/src/main/java/com/necpgame/<service>/service/`

**⚠️ ВАЖНО:** Контракты генерируются прямо в каталоге микросервиса и не удаляются при `mvn clean`!

#### Шаг 2: Создание реализации вручную

Используя шаблоны из [MANUAL-TEMPLATES.md](./MANUAL-TEMPLATES.md), создаёт:

1. **Entities** (`microservices/<service>/src/main/java/com/necpgame/<service>/entity/`)
   - Пример: `AccountEntity.java`, `CharacterEntity.java`
   - С relationships: `@OneToMany`, `@ManyToOne`, `@ManyToMany`
   - С indexes: `@Index(name = "idx_email", ...)`
   - С constraints: `@Column(nullable = false, unique = true)`

2. **Repositories** (`microservices/<service>/src/main/java/com/necpgame/<service>/repository/`)
   - Пример: `AccountRepository.java`
   - extends `JpaRepository<AccountEntity, UUID>`
   - С custom queries: `@Query("SELECT ... FROM ...")`
   - С derived methods: `findByEmail()`, `existsByUsername()`

3. **Controllers** (`microservices/<service>/src/main/java/com/necpgame/<service>/controller/`)
   - Пример: `AuthController.java implements AuthApi`
   - Реализует сгенерированный API Interface
   - Делегирует работу Service

4. **ServiceImpl** (`microservices/<service>/src/main/java/com/necpgame/<service>/service/impl/`)
   - Пример: `AuthServiceImpl.java implements AuthService`
   - Реализует сгенерированный Service Interface
   - Содержит всю бизнес-логику

5. **Liquibase миграции** (`microservices/<service>/src/main/resources/db/changelog/`)
   - Пример: `db.changelog-master.xml`, `001-create-accounts.xml`
   - Версионирование через changeSet с id
   - Идемпотентность: Liquibase отслеживает применённые изменения

#### Шаг 3: Компиляция и тестирование

```bash
# Компиляция
mvn clean compile

# Тестирование
mvn test

# Запуск приложения
mvn spring-boot:run
```

#### Шаг 4: ОБЯЗАТЕЛЬНАЯ процедура обновления implementation-tracker.yaml (ПРИ ЗАВЕРШЕНИИ)

**⚠️ КРИТИЧЕСКИ ВАЖНО:** Этот шаг ОБЯЗАТЕЛЕН после завершения реализации!

**При ЗАВЕРШЕНИИ работы:**

1. **Получить РЕАЛЬНОЕ время из системы:**
   ```powershell
   # Windows
   powershell -Command "Get-Date -Format 'yyyy-MM-dd HH:mm'"
   
   # Linux/Mac
   date '+%Y-%m-%d %H:%M'
   ```

2. **Получить хэш последнего коммита:**
   ```powershell
   git log -1 --format=%h
   ```

3. **Открыть файл:** `../.BRAIN/06-tasks/config/implementation-tracker.yaml`

4. **Найти запись для API** (по api_path)

5. **Обновить поля backend:**
   ```yaml
   backend:
     status: "completed"  # Было: in_progress
     started: "2025-11-07 10:00"
     completed: "2025-11-07 15:00"  # РЕАЛЬНОЕ время!
     agent: "Backend Agent"
     commit: "abc123d"  # Хэш коммита
     notes: ""  # Опционально: примечания
   ```

6. **Сохранить файл**

7. **Сделать коммит обновления tracker:**
   ```powershell
   cd ..
   .BRAIN\scripts\autocommit.ps1 "backend: завершена реализация для api/v1/..."
   cd BACK-GO
   ```

**Зачем это нужно:**
- ФРОНТТАСК проверяет backend.status: "completed" перед началом работы
- Полная видимость статуса реализации
- Отслеживание истории разработки

**Что будет дальше:**
- ФРОНТТАСК увидит backend.status: "completed"
- Начнёт реализацию frontend
- Обновит frontend.status: "in_progress" → "completed"

---

#### Шаг 5: Коммит изменений

```powershell
.\scripts\autocommit.ps1 "feat: Add character creation backend implementation"
```

---

## 📁 Структура проекта

### Структура исходников микросервисов

**Каталог микросервиса:** `microservices/<service>/src/main/java/com/necpgame/<service>/`

```
api/             ← REST API контракты (✨ генерируется)
model/           ← DTOs (✨ генерируется)
service/         ← Service интерфейсы (✨ генерируется)
service/impl/    ← Реализация бизнес-логики (✍️ вручную)
controller/      ← REST контроллеры (✍️ вручную, реализуют API интерфейсы)
entity/          ← JPA сущности (✍️ вручную)
repository/      ← Spring Data репозитории (✍️ вручную)
exception/       ← Исключения и ErrorCode (✍️ вручную)
mapper/          ← MapStruct мапперы (✍️ вручную)
config/          ← Конфигурация Spring/Security (✍️ вручную)
```

**Каталог ресурсов:** `microservices/<service>/src/main/resources/`

```
application.yml              ← Настройки микросервиса
application-docker.yml       ← Настройки для Docker
db/changelog/                ← Liquibase миграции (✍️ вручную)
```

**⚠️ Правила:**
- Контракты (`api/`, `model/`, `service/`) генерируются автоматикой — **не редактировать вручную**.
- Реализация (`controller/`, `service/impl/`, `entity/`, `repository/`, `mapper/`) создаётся вручную по шаблонам.
- Каждому микросервису соответствует собственный Java пакет (`com.necpgame.authservice`, `com.necpgame.characterservice`, ...).

### Миграции БД (`src/main/resources/`):

```
src/main/resources/
├── db/changelog/     ← Liquibase миграции
│   ├── db.changelog-master.xml              # Главный файл
│   └── changes/
│       ├── 001-create-accounts-table.xml
│       ├── 002-create-characters-table.xml
│       ├── 003-add-character-constraints.xml
│       └── ...
│
├── liquibase.properties  ← Конфигурация Liquibase
└── application.yml       ← Spring Boot конфигурация
```

---

## ⚡ Ключевые принципы

### ОБЯЗАТЕЛЬНО:

1. ✅ **OpenAPI = единственный источник истины** - все REST API аннотации определены там
2. ✅ **Генерируй только контракты** - DTOs, API Interfaces, Service Interfaces
3. ✅ **Controllers ОБЯЗАНЫ реализовывать API интерфейсы** (`implements AuthApi`)
4. ✅ **НЕ дублируй Spring MVC аннотации** - они УЖЕ в API интерфейсах!
5. ✅ **Создавай реализацию вручную** - Entities, Repositories, Controllers, ServiceImpl
6. ✅ **Используй шаблоны** из [MANUAL-TEMPLATES.md](./MANUAL-TEMPLATES.md)
4. ✅ **Соблюдай SOLID, DRY, KISS**
5. ✅ **Ограничение размера файла** - максимум 400 строк; сводные трекеры (`implementation-tracker.yaml` и другие общие отчёты) после достижения лимита переносим в новую версию с суффиксом `_0001`, `_0002`, сохраняя ссылку на предыдущую часть и копируя актуальные данные
6. ✅ **Используй Liquibase миграции** для БД (версионирование, отслеживание изменений)
7. ✅ **Используй MapStruct** для маппинга Entity ↔ DTO (автоматическая генерация)
8. ✅ **Именуй модели правильно** - НЕ используй `Character`, `String`, `Number` и др. встроенные типы Java/JS
   - ✅ Используй `GameCharacter` вместо `Character`
   - ✅ Используй `GameEvent` вместо `Event`
   - ✅ Это правило применяется в OpenAPI спецификациях!
9. ✅ **Создавай тестовые данные** (seed данные) - с проверкой существования
10. ✅ **Тестирование** - покрытие не менее 50%
11. ✅ **Не хардкодь данные** - всё хранить в БД
12. ✅ **Проверяй работу API** - все endpoints должны работать
13. ✅ **Коммить изменения** через `.\scripts\autocommit.ps1`

---

## 🔄 Workflow агента

### 1. Получение задачи

```
Создай бекенд для API-SWAGGER/api/v1/auth/character-creation.yaml
```

### 2. Генерация контрактов

```powershell
cd BACK-GO
pwsh -NoProfile -Command "./scripts/validate-openapi.ps1 -ApiSpec ../API-SWAGGER/api/v1/auth/character-creation.yaml"
pwsh -NoProfile -Command "./scripts/generate-openapi-microservices.ps1 -ApiSpec ../API-SWAGGER/api/v1/auth/character-creation.yaml"
```

### 3. Анализ сгенерированных контрактов

Проверяет что создано:
- `microservices/auth-service/src/main/java/com/necpgame/authservice/api/AuthApi.java` - REST контракт
- `microservices/auth-service/src/main/java/com/necpgame/authservice/model/LoginRequest.java` - DTO
- `microservices/auth-service/src/main/java/com/necpgame/authservice/service/AuthService.java` - Service контракт

### 4. Создание реализации

Используя [MANUAL-TEMPLATES.md](./MANUAL-TEMPLATES.md):

**4.1. Entity**
```java
// microservices/auth-service/src/main/java/com/necpgame/authservice/entity/AccountEntity.java
@Entity
@Table(name = "accounts")
public class AccountEntity {
    @Id @GeneratedValue(strategy = GenerationType.UUID)
    private UUID id;
    
    @Column(nullable = false, unique = true)
    private String email;
    
    // ... relationships, timestamps
}
```

**4.2. Repository**
```java
// microservices/auth-service/src/main/java/com/necpgame/authservice/repository/AccountRepository.java
@Repository
public interface AccountRepository extends JpaRepository<AccountEntity, UUID> {
    Optional<AccountEntity> findByEmail(String email);
    boolean existsByEmail(String email);
}
```

**4.3. ServiceImpl**
```java
// microservices/auth-service/src/main/java/com/necpgame/authservice/service/impl/AuthServiceImpl.java
@Service
@RequiredArgsConstructor
public class AuthServiceImpl implements AuthService {
    private final AccountRepository accountRepository;
    
    @Override
    public LoginResponse login(LoginRequest request) {
        // Бизнес-логика
    }
}
```

**4.4. Controller**
```java
// microservices/auth-service/src/main/java/com/necpgame/authservice/controller/AuthController.java
@RestController  // ← Только это! Без @RequestMapping
@RequiredArgsConstructor
public class AuthController implements AuthApi {  // ← ОБЯЗАТЕЛЬНО implements
    private final AuthService authService;
    
    @Override  // ← Все аннотации (@RequestMapping, @RequestParam) УЖЕ в AuthApi!
    public ResponseEntity<LoginResponse> login(LoginRequest request) {
        return ResponseEntity.ok(authService.login(request));
    }
}
```

**⚠️ КРИТИЧЕСКИ ВАЖНО:**
- Контроллер **ОБЯЗАН** реализовывать API интерфейс (`implements AuthApi`)
- **НЕ добавляй** `@RequestMapping`, `@GetMapping`, `@PostMapping` - они УЖЕ в интерфейсе!
- **НЕ добавляй** `@RequestParam`, `@PathVariable`, `@RequestBody` - они УЖЕ в интерфейсе!
- OpenAPI спецификация = единственный источник истины для всех аннотаций!

**4.5. Обработка исключений**

Используем **минималистичный подход** - только 4 доменных исключения:

```java
// ErrorCode enum с маппингом на HTTP статусы
public enum ErrorCode {
    // AUTH - аутентификация/авторизация
    INVALID_CREDENTIALS(HttpStatus.UNAUTHORIZED, "AUTH_001", "Invalid credentials"),
    TOKEN_EXPIRED(HttpStatus.UNAUTHORIZED, "AUTH_002", "Token has expired"),
    ACCESS_DENIED(HttpStatus.FORBIDDEN, "AUTH_004", "Access denied"),
    
    // BIZ - бизнес-логика
    RESOURCE_NOT_FOUND(HttpStatus.NOT_FOUND, "BIZ_001", "Resource not found"),
    RESOURCE_ALREADY_EXISTS(HttpStatus.CONFLICT, "BIZ_002", "Resource already exists"),
    
    // VAL - валидация
    INVALID_INPUT(HttpStatus.BAD_REQUEST, "VAL_001", "Invalid input data"),
    
    // INT - интеграции
    DATABASE_ERROR(HttpStatus.INTERNAL_SERVER_ERROR, "INT_001", "Database error")
}

// Использование в коде
if (!passwordEncoder.matches(password, hash)) {
    throw new AuthException(ErrorCode.INVALID_CREDENTIALS);
}

if (characterRepository.existsByName(name)) {
    throw new BusinessException(ErrorCode.RESOURCE_ALREADY_EXISTS, 
        "Character already exists: " + name);
}
```

**Преимущества:**
- ✅ Нет дублирования кода (одинаковых конструкторов)
- ✅ Единая модель `Error` из OpenAPI для всех ошибок
- ✅ HTTP статус определяется автоматически по `ErrorCode`
- ✅ Легко добавлять новые коды ошибок без новых классов
- ✅ GlobalExceptionHandler преобразует в единый формат

**4.6. Liquibase миграция**
```xml
<!-- microservices/auth-service/src/main/resources/db/changelog/changes/001-create-accounts-table.xml -->
<changeSet id="001-create-accounts-table" author="system">
    <createTable tableName="accounts">
        <column name="id" type="UUID">
            <constraints primaryKey="true" nullable="false"/>
        </column>
        <column name="email" type="VARCHAR(255)">
            <constraints nullable="false" unique="true"/>
        </column>
        <column name="username" type="VARCHAR(100)">
            <constraints nullable="false" unique="true"/>
        </column>
        <column name="password_hash" type="VARCHAR(255)">
            <constraints nullable="false"/>
        </column>
        <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
            <constraints nullable="false"/>
        </column>
        <column name="updated_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
            <constraints nullable="false"/>
        </column>
    </createTable>
    
    <createIndex tableName="accounts" indexName="idx_accounts_email">
        <column name="email"/>
    </createIndex>
    <createIndex tableName="accounts" indexName="idx_accounts_username">
        <column name="username"/>
    </createIndex>
</changeSet>
```

### 5. Компиляция и тестирование

```bash
mvn clean compile
mvn test
mvn spring-boot:run
```

### 6. Проверка API

```bash
# Тест через curl
curl -X POST http://localhost:8080/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email": "test@example.com", "password": "password123"}'
```

### 7. Коммит

```powershell
.\scripts\autocommit.ps1 "feat: Add authentication endpoints with full implementation"
```

---

## 📚 Документация

### Основная документация:
- **[MANUAL-TEMPLATES.md](./MANUAL-TEMPLATES.md)** - шаблоны для ручного создания
- **[БЭКТАСК-ARCHITECTURE.md](./БЭКТАСК-ARCHITECTURE.md)** - архитектура проекта
- **[БЭКТАСК-FAQ.md](./БЭКТАСК-FAQ.md)** - FAQ и примеры
- **[БЭКТАСК-BEST-PRACTICES.md](./БЭКТАСК-BEST-PRACTICES.md)** - критически важные рекомендации из опыта

### Вспомогательная документация:
- **[DOCKER-SETUP.md](./DOCKER-SETUP.md)** - настройка Docker
- **[QUICK-START.md](./QUICK-START.md)** - быстрый старт

### Техническая документация:
- **[templates/STRUCTURE.md](../templates/STRUCTURE.md)** - структура шаблонов
- **[scripts/generate-openapi-microservices.ps1](../scripts/generate-openapi-microservices.ps1)** - скрипт генерации контрактов в микросервисы

---

## 🛠️ Технологии

### Стек 2025:
- **Java 21+** - современная версия Java
- **Spring Boot 3.2+** - фреймворк
- **PostgreSQL 15+** - БД с Spring Data JPA / Hibernate
- **Liquibase** - миграции БД
- **OpenAPI Generator** - генерация контрактов
- **Maven** - сборка проекта
- **Docker** - контейнеризация
- **JUnit 5** - тестирование
- **Logback** - логирование

---

## 📝 Примеры команд для агента

```
# Один API файл
Создай бекенд для API-SWAGGER/api/v1/auth/character-creation.yaml

# Директория с API
Создай бекенд для всех API из API-SWAGGER/api/v1/auth/

# Конкретная фича
Создай бекенд для API-SWAGGER/api/v1/gameplay/combat/cyberpsychosis.yaml
```

---

## ⚠️ Важные правила

### DO ✅:
- ✅ Генерируй контракты через скрипт
- ✅ Создавай реализацию по шаблонам
- ✅ Используй Liquibase для миграций
- ✅ Используй MapStruct для маппинга Entity ↔ DTO
- ✅ Следи за подсказками линтера в Cursor (панель Diagnostics) и устраняй новые предупреждения перед коммитом
- ✅ Пиши тесты (50%+ покрытие)
- ✅ Соблюдай SOLID, DRY, KISS
- ✅ Файлы до 400 строк; сводные отчёты (`implementation-tracker.yaml` и аналоги) при превышении лимита продолжай в новых файлах с суффиксом `_0001`, `_0002` и ссылкой на предыдущие части
- ✅ Именуй модели правильно (GameCharacter, не Character)
- ✅ Коммить через autocommit

### DON'T ❌:
- ❌ Не редактируй сгенерированные контракты вручную (в api/, model/, service/)
- ❌ Не генерируй Entities/Repositories/Controllers/ServiceImpl
- ❌ **НЕ дублируй Spring MVC аннотации в контроллерах** - они УЖЕ в API интерфейсах!
- ❌ **НЕ создавай контроллеры без `implements XxxApi`** - это обязательно!
- ❌ Не хардкодь данные в коде
- ❌ Не создавай файлы больше 400 строк — заверши файл и продолжай в новой версии с суффиксом `_0001`, `_0002`, указывая ссылку на предыдущую часть
- ❌ Не пропускай тесты
- ❌ Не забывай коммитить
- ❌ НЕ редактируй pom.xml, README.md в корне проекта при генерации

---

## 🏗️ Архитектура: OpenAPI как единственный источник истины

### ⚠️ КРИТИЧЕСКИ ВАЖНО ПОНЯТЬ!

**OpenAPI спецификация определяет ВСЕ Spring MVC аннотации для REST API.**

#### ✅ Правильно (OpenAPI First):

```java
// Сгенерированный API интерфейс из OpenAPI
@Validated
public interface FactionsApi {
    @Operation(operationId = "getFactions")
    @RequestMapping(method = RequestMethod.GET, value = "/factions")  // ← Из OpenAPI!
    default ResponseEntity<GetFactions200Response> getFactions(
        @RequestParam(value = "origin", required = false) String origin  // ← Из OpenAPI!
    ) { ... }
}

// Контроллер - ТОЛЬКО реализация, БЕЗ дублирования аннотаций
@RestController
@RequiredArgsConstructor
public class FactionsController implements FactionsApi {  // ← ОБЯЗАТЕЛЬНО!
    
    @Override  // ← Все аннотации наследуются из FactionsApi
    public ResponseEntity<GetFactions200Response> getFactions(String origin) {
        return ResponseEntity.ok(service.getFactions(origin));
    }
}
```

#### ❌ Неправильно (дублирование аннотаций):

```java
// ❌ НЕ ДЕЛАЙ ТАК!
@RestController
@RequestMapping("/factions")  // ❌ Дублирование! Уже есть в FactionsApi
public class FactionsController {  // ❌ Не реализует FactionsApi!
    
    @GetMapping  // ❌ Дублирование! Уже есть в FactionsApi
    public ResponseEntity<...> getFactions(
        @RequestParam(required = false) String origin  // ❌ Дублирование!
    ) { ... }
}
```

### 📊 Преимущества OpenAPI First:

1. **Единственный источник истины** - все маршруты/параметры в OpenAPI
2. **Нет дублирования** - аннотации не повторяются в контроллерах
3. **Type Safety** - компилятор проверяет соответствие контроллера и API
4. **Автосинхронизация** - изменения в OpenAPI → сразу в коде
5. **Невозможно забыть** - компилятор требует реализацию всех методов

### 🔄 Workflow с OpenAPI First:

1. **Изменяем OpenAPI**: Добавляем/меняем endpoint в `character-creation.yaml`
2. **Генерируем контракты**: `pwsh -NoProfile -Command "./scripts/generate-openapi-microservices.ps1 -ApiSpec ../API-SWAGGER/api/v1/character-creation.yaml"`
3. **Компилятор показывает ошибки**: "Method does not override..."
4. **Исправляем контроллер**: Добавляем/меняем метод
5. **Всё работает!** ✅

**Подробнее**: [OPENAPI-CONTRACT-ARCHITECTURE.md](../OPENAPI-CONTRACT-ARCHITECTURE.md)

---

## 🔥 Критически важные рекомендации

**Детальные best practices и решение типичных проблем:** См. [БЭКТАСК-BEST-PRACTICES.md](./БЭКТАСК-BEST-PRACTICES.md)

**Основные моменты:**
1. Enum маппинг: используй `getValue()` вместо `name()`
2. GlobalExceptionHandler: обязательно заполняй `details`
3. Имена полей: используй snake_case в JSON
4. Тестирование: используй lowercase enum значения
5. SecurityUtil: временная заглушка с фиксированным UUID
6. Seed данные: проверь наличие перед тестированием
7. Перезапуск: обязателен после изменений в Service/Controller/Mapper

---

## 🔗 Полезные ссылки

### Внешняя документация:
- [OpenAPI Generator](https://openapi-generator.tech/)
- [Spring Boot Documentation](https://spring.io/projects/spring-boot)
- [Spring Data JPA](https://spring.io/projects/spring-data-jpa)
- [Liquibase](https://www.liquibase.org/)
- [PostgreSQL JDBC](https://jdbc.postgresql.org/)

### Внутренняя документация:
- [DEVELOPMENT-WORKFLOW.md](../../DEVELOPMENT-WORKFLOW.md) - workflow проекта (полная диаграмма воркфлоу)
- [STATUSES-GUIDE.md](../../.BRAIN/06-tasks/config/STATUSES-GUIDE.md) - справочник всех систем статусов
- [WORKFLOW-DETAILS.md](../../.BRAIN/06-tasks/config/WORKFLOW-DETAILS.md) - детальное описание каждого этапа
- [API-SWAGGER/ДУАПИТАСК.MD](../../API-SWAGGER/ДУАПИТАСК.MD) - предыдущий этап (создание заданий)
- [API-SWAGGER/АПИТАСК.MD](../../API-SWAGGER/АПИТАСК.MD) - предыдущий этап (создание API)
- [FRONT-WEB/ФРОНТТАСК.MD](../../FRONT-WEB/ФРОНТТАСК.MD) - следующий этап (реализация frontend)
- [FRONT-WEB/API-GENERATION-README.md](../../FRONT-WEB/API-GENERATION-README.md) - руководство по генерации фронтенда (Orval)

### Системы отслеживания:
- **[implementation-tracker.yaml](../../.BRAIN/06-tasks/config/implementation-tracker.yaml)** - статусы реализации backend/frontend (**БЭКТАСК обновляет backend поля!**)
- [brain-mapping.yaml](../../API-SWAGGER/tasks/config/brain-mapping.yaml) - связи .BRAIN → задания API

---

**Готов к работе! Пришли команду с путём к OpenAPI спецификации - я создам бекенд код на Java Spring Boot.**
