# БЭКТАСК-PROCESS.md

**Детальный процесс работы агента для создания бекенд кода на Java Spring Boot на основе API Swagger спецификаций**

📖 **Навигация:** [БЭКТАСК.MD](./БЭКТАСК.MD) | [БЭКТАСК-REQUIREMENTS.md](./БЭКТАСК-REQUIREMENTS.md) | [БЭКТАСК-FAQ.md](./БЭКТАСК-FAQ.md) | [БЭКТАСК-ARCHITECTURE.md](./БЭКТАСК-ARCHITECTURE.md)

---

## Процесс работы агента

### Шаг 1: Чтение правил и API Swagger

Агент должен:
1. **Проверить готовность API спецификации:** Убедиться, что API Swagger спецификация готова и полная
   - Проверить наличие API Swagger файла в `API-SWAGGER/api/v1/`
   - Проверить, что все endpoints определены
   - Проверить, что модели данных описаны
   - Если спецификация не готова - сообщить об этом и ожидать создания
2. Прочитать `BACK-JAVA/.cursor/rules/back-java-rules.mdc` - понять правила работы с бекендом
3. Прочитать API Swagger спецификацию из `API-SWAGGER/api/v1/*.yaml` полностью
4. Понять структуру API: endpoints, модели данных, параметры, ответы
5. (Опционально) Проверить существующий код в `BACK-JAVA/src/main/java/com/necpgame/backjava/` для примеров стиля и формата
6. **Проверить наличие Docker контейнера:** Убедиться, что PostgreSQL контейнер запущен через `docker-compose up -d` (использовать `BACK-JAVA/docker-compose.yml`)

---

### Шаг 2: Генерация Java Spring Boot кода через OpenAPI Generator

Агент должен:

#### 1. Установить OpenAPI Generator (если не установлен):

**OpenAPI Generator (рекомендуется):**
```bash
# Установка через npm
npm install @openapitools/openapi-generator-cli -g

# Или через Docker
docker pull openapitools/openapi-generator-cli
```

#### 2. Генерировать Java Spring Boot код:

**Используя OpenAPI Generator через Maven:**
```bash
# Генерация Java Spring Boot кода
mvn clean generate-sources
```

**Или через OpenAPI Generator CLI:**
```bash
# Генерация Java Spring Boot кода
openapi-generator-cli generate \
  -i API-SWAGGER/api/v1/auth/character-creation.yaml \
  -g spring \
  -o BACK-JAVA/target/generated-sources/openapi \
  --additional-properties=library=spring-boot,useSpringBoot3=true,useJakartaEe=true
```

**Конфигурация в pom.xml:**
OpenAPI Generator настроен в `pom.xml` и автоматически генерирует код при сборке проекта.

#### 3. Проверить сгенерированный код:

- Проверить, что все модели данных сгенерированы (в `target/generated-sources/openapi/src/main/java/com/necpgame/backjava/model/`)
- Проверить, что все Controllers сгенерированы (в `target/generated-sources/openapi/src/main/java/com/necpgame/backjava/api/`)
- Проверить структуру директорий
- Проверить соответствие Java стандартам

#### 4. Обновить конфигурацию (если нужно):

- Проверить порт сервера (из `servers` в Swagger)
- Настроить аутентификацию (Bearer token)
- Настроить обработку ошибок

---

### Шаг 3: Создание Controllers, Services, Repositories

Агент должен:

#### 1. Использовать сгенерированные Controllers из OpenAPI Generator:

**OpenAPI Generator для Java Spring Boot генерирует:**
- ✅ Controllers (интерфейсы) в `target/generated-sources/openapi/src/main/java/com/necpgame/backjava/api/*Api.java`
- ✅ Models/DTOs (модели данных) в `target/generated-sources/openapi/src/main/java/com/necpgame/backjava/model/*.java`
- ✅ JPA Entities (через кастомный шаблон `Entity.mustache`) в `target/generated-sources/entities/src/main/java/com/necpgame/backjava/entity/*.java`
- ✅ Repositories (через скрипт `generate-repositories.ps1`) в `target/generated-sources/repositories/src/main/java/com/necpgame/backjava/repository/*Repository.java`
- ✅ Конфигурация Spring Boot
- ✅ Валидация через Bean Validation
- ❌ Services - НЕ генерируются (нужно создавать вручную)

#### 2. Инструменты для автоматизации:

**Текущий процесс генерации (одна команда):**
```bash
mvn clean generate-sources
```

**Что генерируется автоматически:**
- ✅ **OpenAPI Generator** - генерирует Controllers и Models/DTOs автоматически
- ✅ **OpenAPI Generator с кастомными шаблонами** - генерирует JPA Entities автоматически
- ✅ **Скрипт generate-repositories.ps1** - генерирует Spring Data JPA Repositories автоматически
- ✅ **Spring Data JPA** - автоматически создает реализации repositories из интерфейсов
- ❌ **Services** - нужно создавать вручную (но это проще благодаря Spring IoC и dependency injection)

**Текущий процесс генерации:**
```bash
# Генерация всего кода одной командой
mvn clean generate-sources
```

**Что генерируется:**
1. **DTOs/Models** - стандартные OpenAPI Generator шаблоны
2. **Controllers** - стандартные OpenAPI Generator шаблоны (интерфейсы)
3. **JPA Entities** - кастомный шаблон `Entity.mustache` через `modelTemplateFiles`
4. **Repositories** - скрипт `scripts/generate-repositories.ps1` через `exec-maven-plugin`

**Пример сгенерированного Repository:**
```java
// Автоматически генерируется скриптом generate-repositories.ps1
@Repository
public interface AccountRepository extends JpaRepository<Account, UUID> {
    Optional<Account> findByEmail(String email);
    boolean existsByEmail(String email);
}

// Spring Data JPA автоматически создает реализацию!
```

**ВАЖНО:** 
- Controllers, Models, Entities и Repositories генерируются автоматически из OpenAPI
- Services нужно создавать вручную, но это проще благодаря Spring IoC и dependency injection
- Все файлы находятся в `target/generated-sources/`, не редактировать вручную

#### 3. Определить структуру кода:

- **Controllers:** Реализовать сгенерированные Controller интерфейсы из OpenAPI Generator
- **Services:** Создать сервисы для бизнес-логики (например, `PersonalNpcService.java`)
- **Repositories:** Создать Spring Data JPA интерфейсы (например, `PersonalNpcRepository.java`)
- **Entities:** Создать JPA Entities на основе моделей из OpenAPI
- **DTOs:** Использовать сгенерированные DTOs из OpenAPI Generator

#### 4. Создать Controllers с соблюдением принципов:

**SOLID:**
- Один Controller - одна ответственность
- Controllers должны быть легко расширяемыми
- Зависимости через Spring dependency injection (@Autowired или конструктор)

**DRY:**
- Переиспользовать общие компоненты через Spring
- Выносить повторяющуюся логику в сервисы
- Использовать общие утилиты

**KISS:**
- Простота и понятность кода
- Избегать излишней абстракции
- Понятные имена переменных и методов

#### 5. Пример структуры Controller:

```java
// src/main/java/com/necpgame/backjava/controllers/PersonalNpcController.java
package com.necpgame.backjava.controllers;

import com.necpgame.backjava.api.PersonalNpcApi;
import com.necpgame.backjava.model.PersonalNpc;
import com.necpgame.backjava.services.PersonalNpcService;
import lombok.RequiredArgsConstructor;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.RestController;

import java.util.List;
import java.util.UUID;

@RestController
@RequiredArgsConstructor
public class PersonalNpcController implements PersonalNpcApi {
    
    private final PersonalNpcService service;
    
    @Override
    public ResponseEntity<List<PersonalNpc>> getPersonalNPCs() {
        List<PersonalNpc> npcs = service.getAllNPCs();
        return ResponseEntity.ok(npcs);
    }
    
    @Override
    public ResponseEntity<PersonalNpc> getPersonalNPC(UUID id) {
        PersonalNpc npc = service.getNPCById(id);
        return ResponseEntity.ok(npc);
    }
    
    @Override
    public ResponseEntity<PersonalNpc> createPersonalNPC(PersonalNpc npc) {
        PersonalNpc created = service.createNPC(npc);
        return ResponseEntity.status(HttpStatus.CREATED).body(created);
    }
}
```

#### 4. Проверить размер файлов:

- **ОБЯЗАТЕЛЬНО:** Каждый файл не должен превышать 400 строк
- Если файл больше 400 строк - **ОБЯЗАТЕЛЬНО** разбить на несколько файлов:
  - Разделить Controllers по методам (GET, POST, PUT, DELETE)
  - Вынести общую логику в сервисы
  - Разделить на подфайлы по функциональности

---

### Шаг 4: Создание миграций БД

Агент должен:

#### 1. Инструменты для генерации миграций:

**ВАЖНО:** НЕТ прямого инструмента для генерации миграций БД из OpenAPI Swagger спецификации!

**Доступные подходы для Java Spring Boot:**

1. **Ручное создание миграций (рекомендуется):**
   - Анализ schemas в OpenAPI спецификации
   - Создание SQL миграций на основе моделей данных
   - Использование идемпотентных команд (CREATE TABLE IF NOT EXISTS)

2. **Hibernate Auto DDL (альтернативный подход, только для разработки):**
   - Можно использовать `hibernate.hbm2ddl.auto=update` для автоматической генерации схемы из JPA Entities
   - **НЕ подходит для продакшена** - только для разработки
   - Рекомендуется использовать Flyway для продакшена

3. **Flyway/Liquibase** - инструменты для управления миграциями:
   - Flyway (рекомендуется) - применяет миграции автоматически при запуске Spring Boot
   - Миграции находятся в `src/main/resources/db/migration/`
   - Формат: `V{версия}__{название}.sql`

**ВАЖНО:** Нет прямого инструмента для генерации миграций из OpenAPI. Нужно анализировать schemas вручную и создавать миграции вручную.

#### 2. Создать миграции для схемы БД:

**Используя Flyway:**
```bash
# Создание миграции вручную
# Создать файл: src/main/resources/db/migration/V{версия}__{название}.sql
# Пример: V6__create_personal_npc_table.sql
```

**Структура миграций Flyway:**
- Миграции находятся в `src/main/resources/db/migration/`
- Название: `V{версия}__{название}.sql` (например: `V6__create_personal_npc_table.sql`)
- Flyway применяет миграции автоматически при запуске Spring Boot приложения

#### 3. Написать SQL миграцию с идемпотентностью:

**ВАЖНО:** Миграции должны быть идемпотентными (можно применять несколько раз без ошибок)

**Процесс создания миграции:**
1. Проанализировать schemas в OpenAPI спецификации
2. Определить структуру таблиц на основе моделей данных
3. Создать CREATE TABLE с проверкой существования
4. Добавить индексы, constraints, foreign keys

```sql
-- src/main/resources/db/migration/V6__create_personal_npc_table.sql
-- Создание таблицы с проверкой существования
CREATE TABLE IF NOT EXISTS personal_npc (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name VARCHAR(255) NOT NULL,
    owner_id UUID NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW(),
    FOREIGN KEY (owner_id) REFERENCES accounts(id) ON DELETE CASCADE
);

-- Создание индекса с проверкой существования
CREATE INDEX IF NOT EXISTS idx_personal_npc_owner_id ON personal_npc(owner_id);
```

**ВАЖНО:** Flyway не использует отдельные файлы для отката (down миграции). Для отката нужно создать новую миграцию с обратными операциями.

#### 4. Проверить размер файлов миграций:

- **ОБЯЗАТЕЛЬНО:** Каждый файл миграции не должен превышать 400 строк
- Если миграция больше 400 строк - разбить на несколько миграций:
  - Разделить по таблицам
  - Разделить по функциональности

#### 4. Создать тестовые данные (seed данные):

**ВАЖНО:** Создавать seed данные только если их изначально не было в БД. Проверять существование данных перед заливкой!

**Способ 1: Отдельная seed миграция**
```sql
-- src/main/resources/db/migration/V7__seed_personal_npc_data.sql
-- Создание тестовых данных с проверкой существования
DO $$
BEGIN
    -- Проверка наличия данных перед вставкой
    IF NOT EXISTS (SELECT 1 FROM personal_npc LIMIT 1) THEN
        INSERT INTO personal_npc (id, name, owner_id) VALUES
        ('00000000-0000-0000-0000-000000000001', 'Test NPC 1', '00000000-0000-0000-0000-000000000001'),
        ('00000000-0000-0000-0000-000000000002', 'Test NPC 2', '00000000-0000-0000-0000-000000000001');
    END IF;
END $$;
```

**ВАЖНО:** Flyway не использует отдельные файлы для отката. Для отката seed данных нужно создать новую миграцию с DELETE операциями.

**Способ 2: Отдельный seed скрипт (Java)**
```java
// src/main/java/com/necpgame/backjava/seed/PersonalNpcSeeder.java
package com.necpgame.backjava.seed;

import com.necpgame.backjava.repositories.PersonalNpcRepository;
import lombok.RequiredArgsConstructor;
import org.springframework.boot.CommandLineRunner;
import org.springframework.stereotype.Component;

@Component
@RequiredArgsConstructor
public class PersonalNpcSeeder implements CommandLineRunner {
    
    private final PersonalNpcRepository repository;
    
    @Override
    public void run(String... args) {
        // Проверка наличия данных
        if (repository.count() > 0) {
            return; // Данные уже есть
        }
        
        // Создание тестовых данных
        // ... создание и сохранение тестовых NPC
    }
}
```

**Применение seed данных:**
```bash
# Через Flyway (автоматически при запуске Spring Boot)
mvn spring-boot:run

# Или через CommandLineRunner (автоматически при запуске)
```

**ВАЖНО:** В Spring Boot можно использовать `@Component` с `CommandLineRunner` для автоматического выполнения seed данных при запуске.

#### 5. Применить миграции:

```bash
# Применение миграций через Flyway (автоматически при запуске Spring Boot)
mvn spring-boot:run

# Или через Maven плагин Flyway
mvn flyway:migrate
```

**ВАЖНО:** Flyway применяет миграции автоматически при запуске Spring Boot приложения. Миграции находятся в `src/main/resources/db/migration/`.

---

### Шаг 5: Валидация и проверка

Агент должен:

1. **Проверить размер файлов:**
   - Каждый файл не должен превышать 400 строк
   - Если файл больше 400 строк - разбить на несколько файлов
   - Убедиться, что разбиение логично и все связи сохранены

2. **Проверить соответствие принципам:**
   - SOLID: один Controller/Service/Repository - одна ответственность
   - DRY: переиспользование кода, отсутствие дублирования
   - KISS: простота и понятность кода

3. **Проверить Java стандарты:**
   - Форматирование кода через IDE или Spotless
   - Линтинг через Checkstyle или SpotBugs
   - Сборка через Maven (`mvn clean install`)

4. **Проверить структуру директорий:**
   - Соответствие иерархии API-SWAGGER
   - Логическая организация файлов
   - README.md в директориях (если нужно)

5. **Проверить обработку ошибок:**
   - Все ошибки обрабатываются корректно
   - Есть логирование ошибок
   - Есть правильные HTTP коды ответов

6. **Проверить работу с БД:**
   - Использование @Transactional для критических операций
   - Правильная обработка ошибок БД через Spring Data JPA
   - Управление соединениями через Spring (автоматически)

7. Проверить соответствие API Swagger спецификации:
   - Все endpoints реализованы правильно
   - Все модели данных соответствуют спецификации
   - Все параметры и ответы соответствуют спецификации

8. **Проверить работу API endpoints:**
   - Запустить бекенд сервер
   - Протестировать все endpoints через curl/Postman
   - Убедиться, что все endpoints доступны и работают
   - Проверить, что миграции применены и БД работает
   - Проверить, что тестовые данные (seed данные) созданы (если нужно)

9. **Проверить готовность для фронтенда:**
   - Убедиться, что все API endpoints работают
   - Проверить CORS настройки для работы с фронтендом
   - Убедиться, что API доступен на `http://localhost:8080/api/v1/`
   - Проверить, что можно протестировать через `curl http://localhost:8080/api/v1/health`

---

### Шаг 6: Финализация

Агент должен:
1. Проверить, что бекенд код создан и сохранен
2. Проверить, что все файлы соответствуют ограничениям размера (400 строк)
3. Убедиться, что код соответствует принципам SOLID, DRY, KISS
4. Проверить соответствие иерархии директорий API-SWAGGER
5. **Проверить готовность для фронтенда:**
   - Убедиться, что все API endpoints работают
   - Проверить, что бекенд можно протестировать через curl/Postman
   - Убедиться, что API доступен на `http://localhost:8080/api/v1/`
   - Проверить, что миграции применены и БД работает
   - Проверить, что тестовые данные созданы (если нужно)
6. **ОБЯЗАТЕЛЬНО сделать коммит** для сохранения истории работы

---

### Шаг 7: Коммит изменений

**ОБЯЗАТЕЛЬНО:** После завершения работы над заданием нужно сделать коммит для сохранения истории и лучшего контроля действий агентов.

Агент должен:
1. Использовать скрипт автокоммита: `scripts/autocommit.ps1` (Windows) или `scripts/autocommit.sh` (Linux/Mac)
2. Выполнить команду из корня репозитория:
   ```bash
   # Windows
   .\scripts\autocommit.ps1 "Создан бекенд код для API: personal-npc-tool"
   
   # Linux/Mac
   ./scripts/autocommit.sh "Создан бекенд код для API: personal-npc-tool"
   ```
3. Если сообщение не указано, скрипт сгенерирует его автоматически на основе измененных файлов
4. Коммит должен включать:
   - Сгенерированный Java Spring Boot код (в `target/generated-sources/openapi/`)
   - Реализованные Controllers (в `src/main/java/com/necpgame/backjava/controllers/`)
   - Созданные services (в `src/main/java/com/necpgame/backjava/services/`)
   - Созданные repositories (в `src/main/java/com/necpgame/backjava/repositories/`)
   - Созданные entities (в `src/main/java/com/necpgame/backjava/entities/`)
   - Созданные миграции (в `src/main/resources/db/migration/`)

**Зачем это нужно:**
- Сохранение истории работы агентов
- Возможность отслеживания изменений
- Лучший контроль действий агентов
- Возможность отката изменений при необходимости

---

📖 **Навигация:** [БЭКТАСК.MD](./БЭКТАСК.MD) | [БЭКТАСК-REQUIREMENTS.md](./БЭКТАСК-REQUIREMENTS.md) | [БЭКТАСК-FAQ.md](./БЭКТАСК-FAQ.md) | [БЭКТАСК-ARCHITECTURE.md](./БЭКТАСК-ARCHITECTURE.md)

